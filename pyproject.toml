[project]
name = "jevhttp"
version = "0.1.0"
description = "Synchronous HTTP client and structured web decision toolkit for OpenAI-compatible models"
readme = "README.md"
authors = [
    { name = "XUEHANGAI", email = "xuehang.ai@outlook.com" }
]
requires-python = ">=3.12"
dependencies = [
    "openai>=3.16.2",
    "pydantic>=2.7",
    "requests>=2.34.2",
]
